import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

//Components
import ImageCleaner from './components/ImageCleaner/App';

ReactDOM.render(
  <React.StrictMode>
    <ImageCleaner />
  </React.StrictMode>,
  document.getElementById('root')
);